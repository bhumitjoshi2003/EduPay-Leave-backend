package com.indraacademy.ias_management.entity;

/** Deliberately flat and small for Phase 1 — see the Support Ticket Phase 1 report. */
public enum SupportTicketCategory {
    LOGIN_ACCOUNT,
    ATTENDANCE,
    LEAVE,
    TIMETABLE,
    NOTIFICATIONS,
    FEES_PAYMENTS,
    APP_WEBSITE,
    OTHER
}
